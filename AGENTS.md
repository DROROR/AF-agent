See `CLAUDE.md` (this directory) for the authoritative architecture, safety
rules, and permanent brand rules that govern every change to this repo.
This file exists only so AI tools that specifically look for `AGENTS.md`
(rather than `CLAUDE.md`) find the same instructions - it intentionally
does not duplicate them.

Note: `apps/web/AGENTS.md` is a different, unrelated file - it is
auto-generated by the Next.js dev server itself (see its own header
comment) and documents that specific app's own framework version, not this
project's engineering rules.
